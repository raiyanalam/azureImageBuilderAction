
on:
  push


jobs:
  checkoutAndUpload:
    name: checkoutAndUpload
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - run: |
        ls -ltr
        pwd
    - name: Upload artifact
      uses: actions/upload-artifact@v1.0.0
      with:        
        name: repo-readme
        path: ./README.md
    - name: Download artifact
      uses: actions/download-artifact@v1.0.0
      with:        
        name: repo-readme 
        #path: 
